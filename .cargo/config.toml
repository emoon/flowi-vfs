[alias]
# Regenerate the committed codegen from api/*.def. The codegen crate is its own
# workspace, so it needs an explicit manifest path; the alias hides that.
regen = "run --quiet --manifest-path codegen/Cargo.toml"
regen-check = "test --manifest-path codegen/Cargo.toml"
